# git Praxis

Git Rebase (squash)
===================

Merge multiple commits into one:

    git rebase -i e17d4de

Last 5 commits:

    git rebase -i HEAD~5


Vim
===

:Gwrite Stage the current file to the index

:Gcommit commit
